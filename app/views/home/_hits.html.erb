<% hits.each do |hit| %>
<div class="hit">
   <p class="doc-name"><%= trim_title(hit[:name]) %></p>
   <p><label>Author: </label><%= author(hit[:author]) %></p>
   <p><label>Published: </label><%= pub_dates(hit[:years]) %></p>
</div>
<% end %>